# Vim 配置使用指南

> 本目录包含两个 Vim 配置文件版本和完整的使用文档

---

## 📁 文件说明

### vimrc1 - 功能完整版

**特点**: 功能丰富，适合需要完整 IDE 体验的开发者

**文件大小**: ~11 KB

**主要特性**:
- ✅ 语法高亮和自动缩进
- ✅ 代码补全和括号自动匹配
- ✅ F5 一键编译运行
- ✅ F8 一键调试
- ✅ CTags 标签浏览
- ✅ Tablist 插件（文件标签页）
- ✅ MiniBufExpl 插件（缓冲区管理）
- ✅ 自动插入文件头
- ✅ 智能补全

**适用场景**:
- C/C++ 开发
- Python 开发
- Java 开发
- Shell 脚本开发

---

### vimrc2 - 精简版

**特点**: 配置简洁，轻量快速

**文件大小**: ~10 KB

**主要特性**:
- ✅ UTF-8 编码支持
- ✅ 语法高亮
- ✅ 智能补全
- ✅ 文件类型检测
- ✅ 自动缩进
- ✅ 状态行美化

**适用场景**:
- 快速编辑
- 服务器远程编辑
- 轻量级需求

---

## 🚀 快速开始

### 安装配置文件

```bash
# 进入 Vim 配置目录
cd vim

# 选择配置版本

# 方式 1: 使用完整版 (推荐)
cp vimrc1 ~/.vimrc

# 方式 2: 使用精简版
cp vimrc2 ~/.vimrc

# 重新加载配置
source ~/.vimrc

# 重启 Vim
vim
```

### 备份现有配置

```bash
# 如果已有 .vimrc，先备份
mv ~/.vimrc ~/.vimrc.backup

# 然后复制新配置
cp vimrc1 ~/.vimrc
```

---

## ⌨️ 快捷键说明（vimrc1）

### 编译和运行

| 快捷键 | 功能 | 适用文件类型 |
|--------|------|-------------|
| `F5` | 编译并运行 | C/C++, Java, Shell |
| `F8` | 启动 GDB 调试 | C/C++ |

### 编辑操作

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+A` | 全选 + 复制 |
| `Ctrl+C` | 复制（选中模式下） |
| `F2` | 删除空行 |
| `Ctrl+F2` | 垂直比较文件 |
| `Alt+F2` | 新建标签页 |
| `F3` | 打开当前目录文件列表 |
| `Ctrl+F3` | 打开文件树 |
| `F12` | 代码格式化 |

### 代码补全

| 输入 | 自动补全 |
|------|---------|
| `(` | 自动补全为 `()` |
| `)` | 智能补全右括号 |
| `{` | 自动补全为 `{\n}` |
| `}` | 智能补全右大括号 |
| `[` | 自动补全为 `[]` |
| `]` | 智能补全右中括号 |
| `"` | 自动补全为 `""` |
| `'` | 自动补全为 `''` |

### 自定义命令

| 命令 | 功能 |
|------|------|
| `<leader>w` | 保存文件 |
| `<leader>f` | 查找文件 |

**注**: `<leader>` 默认为 `\` 键

---

## 🎨 语法高亮主题

### 内置主题

vimrc1 支持以下主题切换（在配置文件中取消注释）：

```vim
"colorscheme murphy      " 深色主题
"colorscheme desert      " 沙漠主题
"colorscheme evening     " 晚间主题
"colorscheme morning     " 晨间主题
```

### 自定义主题

```vim
" 在 .vimrc 中添加
colorscheme your_theme_name
```

---

## 🔧 插件配置

### CTags - 代码标签

**功能**: 快速跳转到函数/变量定义

**使用**:
```bash
# 生成标签文件
ctags -R .

# 在 Vim 中跳转
Ctrl+]    # 跳转到定义
Ctrl+T    # 返回
```

**快捷键**:
- `:TlistOpen` - 打开标签列表
- `:TlistClose` - 关闭标签列表

**配置**:
```vim
let Tlist_Sort_Type = "name"          # 按名称排序
let Tlist_Use_Right_Window = 1        # 在右侧显示
let Tlist_Show_One_File = 1           # 只显示当前文件的标签
let Tlist_Exit_OnlyWindow = 1         # 只有一个窗口时退出
```

### Tablist - 标签页

**功能**: 在标签页中打开多个文件

**快捷键**:
- `:tabnew` - 新建标签页
- `:tabclose` - 关闭当前标签页
- `:tabn` - 下一个标签
- `:tabp` - 上一个标签
- `gt` - 跳到下一个标签
- `gT` - 跳到上一个标签

### MiniBufExpl - 缓冲区管理

**功能**: 管理打开的缓冲区

**配置**:
```vim
let g:miniBufExplMapWindowNavVim = 1     # 使用 Vim 键绑定
let g:miniBufExplMapWindowNavArrows = 1  # 使用箭头键导航
let g:miniBufExplMapCTabSwitchBufs = 1   # 使用 Ctrl+Tab 切换
```

---

## 📝 自动文件头

vimrc1 会自动为新文件插入文件头信息。

### 支持的文件类型

| 文件类型 | 扩展名 | 模板内容 |
|---------|--------|---------|
| Shell | `.sh` | 脚本信息、作者、邮箱、创建时间 |
| C/C++ | `.c`, `.cpp`, `.h`, `.hpp` | 文件信息、作者、邮箱、创建时间 |
| Java | `.java` | 文件信息、作者、邮箱、创建时间 |

### 模板示例

**Shell 文件头**:
```bash
#########################################################################
# File Name: script.sh
# Author: Boil
# mail: free22858@live.com
# Created Time: Sat Jan 31 10:30:00 2025
#########################################################################
#!/bin/bash
```

**C/C++ 文件头**:
```cpp
/*************************************************************************
    > File Name: main.cpp
    > Author: Boil
    > Mail: free22858@live.com
    > Created Time: Sat Jan 31 10:30:00 2025
 ************************************************************************/
```

### 自定义文件头

编辑 `vimrc1` 中的 `SetTitle()` 函数：

```vim
func SetTitle()
    if &filetype == 'sh'
        call setline(1,"\#########################################################################")
        call append(line("."), "\# File Name: ".expand("%"))
        call append(line(".")+1, "\# Author: Your Name")
        call append(line(".")+2, "\# mail: your@email.com")
        call append(line(".")+3, "\# Created Time: ".strftime("%c"))
        call append(line(".")+4, "\#########################################################################")
        call append(line(".")+5, "\#!/bin/bash")
        call append(line(".")+6, "")
    endif
endfunc
```

---

## 🔍 搜索和替换

### 基本搜索

| 命令 | 功能 |
|------|------|
| `/pattern` | 向前搜索 |
| `?pattern` | 向后搜索 |
| `n` | 下一个匹配 |
| `N` | 上一个匹配 |
| `*` | 搜索光标下的单词 |
| `#` | 搜索光标下的单词（反向） |

### 高级搜索

| 命令 | 功能 |
|------|------|
| `/\<pattern\>` | 匹配整个单词 |
| `/pattern\c` | 忽略大小写 |
| `/pattern\C` | 区分大小写 |

### 替换

| 命令 | 功能 |
|------|------|
| `:s/old/new/` | 替换第一个 |
| `:s/old/new/g` | 替换行内所有 |
| `:%s/old/new/g` | 替换文件内所有 |
| `:%s/old/new/gc` | 替换所有（需确认） |

---

## 📋 常用命令

### 文件操作

| 命令 | 功能 |
|------|------|
| `:e filename` | 打开文件 |
| `:w` | 保存 |
| `:q` | 退出 |
| `:wq` | 保存并退出 |
| `:q!` | 不保存退出 |
| `:x` | 保存并退出（如果已修改） |

### 窗口操作

| 命令 | 功能 |
|------|------|
| `:split` | 水平分割窗口 |
| `:vsplit` | 垂直分割窗口 |
| `Ctrl+w h` | 移动到左侧窗口 |
| `Ctrl+w j` | 移动到下方窗口 |
| `Ctrl+w k` | 移动到上方窗口 |
| `Ctrl+w l` | 移动到右侧窗口 |
| `Ctrl+w q` | 关闭当前窗口 |

### 缓冲区操作

| 命令 | 功能 |
|------|------|
| `:ls` | 列出所有缓冲区 |
| `:b1` | 跳转到第 1 个缓冲区 |
| `:bn` | 下一个缓冲区 |
| `:bp` | 上一个缓冲区 |
| `:bd` | 删除当前缓冲区 |

---

## 🎯 编程语言支持

### C/C++

**编译和运行**:
```bash
# 在 Vim 中按 F5
# 相当于执行:
g++ main.cpp -o main
./main
```

**调试**:
```bash
# 在 Vim 中按 F8
# 相当于执行:
g++ main.cpp -g -o main
gdb ./main
```

**代码格式化**:
```vim
# 按 F12 自动格式化
```

### Python

**运行**:
```bash
# 按 F5 自动运行
python script.py
```

### Java

**编译和运行**:
```bash
# 按 F5 自动编译运行
javac Main.java
java Main
```

---

## 🔧 高级配置

### 自定义快捷键

在 `.vimrc` 中添加：

```vim
" 示例: 按 F4 运行 make
map <F4> :make<CR>

" 示例: 按 Ctrl+s 保存
map <C-s> :w<CR>
imap <C-s> <Esc>:w<CR>
```

### 安装插件

#### 方式 1: 使用 vim-plug

```bash
# 安装 vim-plug
curl -fLo ~/.vim/autoload/plug.vim --create-dirs \
    https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim

# 在 .vimrc 中配置
call plug#begin('~/.vim/plugged')
Plug 'preservim/nerdtree'
Plug 'airblade/vim-gitgutter'
call plug#end()

# 安装插件
:PlugInstall
```

#### 方式 2: 使用 Vundle

```bash
# 安装 Vundle
git clone https://github.com/VundleVim/Vundle.vim.git ~/.vim/bundle/Vundle.vim

# 在 .vimrc 中配置
set nocompatible
filetype off
set rtp+=~/.vim/bundle/Vundle.vim
call vundle#begin()
Plugin 'VundleVim/Vundle.vim'
Plugin 'tpope/vim-fugitive'
call vundle#end()
filetype plugin indent on

# 安装插件
:PluginInstall
```

---

## ⚠️ 常见问题

### 问题 1: F5 不能编译运行

**原因**: 文件类型不支持或编译器未安装

**解决**:
```bash
# 检查编译器是否安装
which gcc
which g++
which python

# 检查文件类型
:set filetype
```

### 问题 2: CTags 不工作

**原因**: 未安装 CTags 或未生成标签文件

**解决**:
```bash
# 安装 CTags
sudo apt-get install ctags  # Ubuntu/Debian
brew install ctags           # macOS

# 生成标签
ctags -R .

# 在 Vim 中重新加载
:TlistUpdate
```

### 问题 3: 中文显示乱码

**解决**:
```vim
" 在 .vimrc 中添加
set encoding=utf-8
set termencoding=utf-8
set fileencoding=utf-8
set fileencodings=utf-8,gbk,gb2312
```

### 问题 4: 颜色不显示

**原因**: 终端不支持颜色或配置错误

**解决**:
```bash
# 检查终端颜色支持
echo $TERM

# 设置终端类型
export TERM=xterm-256color
```

### 问题 5: 退格键不工作

**解决**:
```vim
" 在 .vimrc 中添加
set backspace=indent,eol,start
set whichwrap+=<,>,h,l
```

---

## 📚 学习资源

### 官方文档
- [Vim 官方网站](https://www.vim.org/)
- [Vim 在线文档](https://vimdoc.sourceforge.net/)
- [Vim Tips Wiki](http://vim.wikia.com/)

### 教程
- [Vim Adventures](https://vim-adventures.com/) - 互动式游戏教程
- [OpenVim](https://openvim.com/) - 在线学习
- [Vim Galore](https://vim.galore/) - 深入理解 Vim

### 插件资源
- [VimAwesome](https://vimawesome.com/) - Vim 插件大全
- [GitHub Vim](https://github.com/topics/vim) - Vim 相关项目

### 配置示例
- [dotfiles.org](https://dotfiles.org/) - 用户配置分享
- [GitHub Search](https://github.com/search?q=vimrc) - 搜索 .vimrc 配置

---

## 🔄 更新日志

### v1.0.0 (2025-01-31)

**新增**:
- Vim配置使用指南.md 完整文档
- vimrc1 文件头注释
- 快捷键说明
- 常见问题解答

**特性**:
- 两个版本的配置文件
- 详细的快捷键映射
- 插件配置说明
- 编程语言支持

---

## 📞 获取帮助

### Vim 内置帮助

```vim
:help            # 打开帮助
:help user-manual # 用户手册
:help quickref   # 快速参考
:help tutorial   # 教程
:help <command>  # 查看命令帮助（如 :help :split）
```

### 键位图

```vim
:help key-notation  # 查看键位表示法
```

---

**最后更新**: 2025-01-31
**维护者**: rendu-env
**相关问题**: 请提交 GitHub Issue
